#include <stdio.h>
#include <cs50.h> 

int main (void)
{

     
}

/*
espaço + .. (mover pra pasta anterior)
cd + nome da pasta ou caminho (mudar de pasta)
cd volta tudo
cp (copiar)
ls (listar)
mkdir (criar pasta)
mv (mover fisicamente (adicionar "/" pra garantir)e renomear)
rm (remover)
rmdir (remover pasta)
clear (limpar)
pwd (print working directory → mostrar o caminho da pasta atual
touch (criar arquivo vazio)
code (abrir VSCode na pasta/arquivo atual)
*/
//compilar: gcc linux.c -lcs50 -o linux